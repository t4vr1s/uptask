<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  body.login
    .contenedor-formulario
      h1 Iniciar sesión en UpTask

      include partials/mensajes
      
      form#fomulario.caja.caja-login(method="post" action="/iniciar-sesion")
        .campo
          label(for="email") Email
          input(type="email" name="email" id="email" value= email placeholder="Tu email" required)
        .campo
          label(for="password") Password 
          input(type="password" name="password" id="password" value= password placeholder="Tu password" required)
        .campo 
          label(for="email")  user="eescobar@computin.cl"
        .campo
          label(for="password")  pass="123"
        .campo.enviar
          input.boton(type="submit" value="Iniciar Sesión")
        .campo.acciones
          a(href="/crear-cuenta") Registrate gratis
          
          a(href="/reestablecer") ¿Olvidaste tu contraseña?
    